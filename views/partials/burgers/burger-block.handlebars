{{#if devoured}}

 <li>
    {{burger_name}}
  </li>

{{else}}
    <li class="not-yet">
      {{burger_name}}
      <button class="eat-burger" data-id="{{this.id}}">Devour It!</button>
    </li>

{{/if}}


